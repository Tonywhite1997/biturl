version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: biturl-postgres
    restart: always
    environment:
      POSTGRES_USER: biturl
      POSTGRES_PASSWORD: biturl
      POSTGRES_DB: biturl
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: biturl-redis
    restart: always
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: biturl-rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  clickhouse:
    image: clickhouse/clickhouse-server:latest
    container_name: biturl-clickhouse
    restart: always
    environment:
      CLICKHOUSE_DB: biturl
      CLICKHOUSE_USER: biturl
      CLICKHOUSE_PASSWORD: biturl
    ports:
      - "8123:8123"
      - "9000:9000"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - clickhouse_logs:/var/log/clickhouse-server

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
  clickhouse_data:
  clickhouse_logs:
